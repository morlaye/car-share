# General
.DS_Store
*.log
.vscode/

# Build results
[Bb]in/
[Oo]bj/
dist/
build/

# .NET
*.user
*.userosscache
*.sln.docstates
*.ncrunch*
*.suo
project.lock.json
project.fragment.lock.json
artifacts/

# Node.js / Front-end
node_modules/
.next/
out/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# SQL Server
*.mdf
*.ldf

# Local settings
appsettings.local.json
appsettings.Development.json
!src/API/appsettings.Development.json # Keep the template for dev reference, but usually ignored in strict teams. 
# For this solo project, keeping the generic dev settings in git is helpful for the VM setup.
# Modify if sensitive data is added.

# Coolify
coolify/
